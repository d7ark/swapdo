{
    "name": "swapdo",
    "version": "0.0.1",
    "description": "ToDo list using koa, mongo, flow, tape, istanbul",
    "main": "server.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "coverage-test": "echo \"Error: no coverage test specified\" && exit 2"
    },
    "author": "Darek Antkowicz",
    "license": "ISC",
    "dependencies": {
        "koa": "2.3.0"
    },
    "devDependencies": {
        "flow-bin": "0.53.1",
        "istanbul": "0.4.5",
        "tape": "4.8.0"
    }
}
